import { lib, game, ui, get, ai, _status } from "../../extension/noname.js";

const card = {
	PSsp_blank: {
		type: null,
		ai: {
			basic: {
				useful: 0,
				value: 0.1,
			},
		},
	},
}

export default card
